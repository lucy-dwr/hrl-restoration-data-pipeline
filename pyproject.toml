[build-system]
requires = ["hatchling==1.27.0"]
build-backend = "hatchling.build"

[project]
name = "hrl-restoration-data-pipeline"
version = "0.3.0"
description = "Deterministic local HRL restoration submission validation and promotion"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "geopandas==1.0.1",
  "pyogrio==0.10.0",
  "shapely==2.0.6",
  "pyproj==3.7.0",
  "linkml==1.11.1",
  "linkml-runtime==1.11.1",
  "jinja2==3.1.4",
]

[project.optional-dependencies]
pdf = ["reportlab==4.2.5"]
test = ["pytest==8.3.4", "pytest-cov==6.0.0", "reportlab==4.2.5"]

[project.scripts]
hrl-pipeline = "hrl_restoration_pipeline.cli:main"

[tool.hatch.build.targets.wheel]
packages = ["src/hrl_restoration_pipeline"]
force-include = { "schema-snapshots/hrl-restoration-schema/v1.3.1" = "hrl_restoration_pipeline/_schema_snapshot/hrl-restoration-schema/v1.3.1" }

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
